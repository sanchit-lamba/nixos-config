# Home Manager module for winapps configuration
{ lib, pkgs, config, inputs, ... }:

{
  # This is a home-manager module, not a flake
  # The winapps functionality should be configured at the system level
  # in configuration.nix or as part of the nixosConfiguration modules
  
  # If you need winapps, add it to the main flake inputs and configure it
  # in the nixosConfiguration modules instead of here
  
  # For now, this is an empty home-manager module
  # You can add home-manager specific configurations here
}
